const initialState = {
    loggedIn: false,
    logginIn: false,
    username: '',
    email: '',
    password: '',
    id: ''
}

export default (state = initialState, action) => {
    switch (action.type) {
        case 
    }
}